# Taak T-3 — Publieke HTML cachen

Zaak: 2026-09-13-camperstaan · Finding: F-002 · Werklijst #: 3

## Packet

| Veld | Inhoud |
| --- | --- |
| URL(s) | `/`, `/plaatsen/kampen-burgemeester-berghuisplein` (anoniem). Niet: `/inloggen`, `/dashboard`, `/betalen`, `/api/*` |
| Platform | code (Next.js / Vercel) |
| Huidige staat | `cache-control: private, no-cache, no-store, max-age=0, must-revalidate`. `x-vercel-cache: MISS` bij herhaalde GET. Geen `set-cookie` op home in deze meting. |
| Gewenste staat | Anonieme catalogus-HTML: `public` met s-maxage (bijv. 300–3600 s, stale-while-revalidate toegestaan). Auth-routes blijven private. Tweede GET op preview toont CDN HIT of minstens geen `no-store`. |
| Buiten scope | Cache van ingelogde eigenaarsomgeving; edge-config voor gepersonaliseerde HTML |
| Herstel | Revert cache-headers; check dat login nog `private` is |
| Acceptatie | `curl -sI` preview home + listing: geen `no-store` op die twee. Login/dashboard nog steeds niet publiek gecached. |
| Werkspiegel vs productie | Headers eerst op preview. Live = Eigenaren ná Toets 2 |

## Mag niet

Live, mail, geld. Scope buiten dit packet.
